br
br
br
.row
    .col-lg-6.col-lg-offset-3
        ul.breadcrumb
            li Attributes
.row(ng-init="init()")
    .col-lg-2
        button.btn.btn-info(type="submit",ng-click="createExternalOperation()") ADD OPERATION
    .col-lg-10
        table.table(ng-table="externalOperationsTableParams", show-filter="true")
            tr(ng-repeat="opr in $data")
                td(title="'Name'", filter="{ name: 'text'}",sortable="'name'")
                    a(ng-href="external-operations/{{opr.id}}") {{opr.name}}
                td(title="'Id'") {{opr.id}}